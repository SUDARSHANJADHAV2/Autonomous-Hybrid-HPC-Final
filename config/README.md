# Configuration

`config.yaml` is the working configuration. `config.example.yaml` is the template for copying into a new installation. Do not put passwords, API keys, private SSH keys, or cloud credentials here; use environment variables, Ansible Vault, AWS profiles/roles, or an external secret manager.
