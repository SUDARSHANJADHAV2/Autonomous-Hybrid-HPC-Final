- name: Harden SSH on the login node
  ansible.builtin.copy:
    dest: /etc/ssh/sshd_config.d/99-hpc-hardening.conf
    owner: root
    group: root
    mode: '0644'
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      PermitRootLogin no
  notify: restart ssh
